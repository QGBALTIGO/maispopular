services:
  bot:
    build: .
    restart: unless-stopped
    env_file: .env
    environment:
      DATABASE_PATH: /app/data/bot.sqlite3
    volumes:
      - soupopular-data:/app/data
    stop_grace_period: 45s
    security_opt:
      - no-new-privileges:true

volumes:
  soupopular-data:
